{
  "name": "DraughtsJS",
  "version": "0.1.0",
  "description": "Draughts Application built with NEO4J, NodeJS, AngularJS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified! Configure in package.json\" && exit 1"
  },
  "dependencies": {
    "express": "3.2.6",
    "neo4j-driver": "*",
    "promise":"*"
    	
  }
 }